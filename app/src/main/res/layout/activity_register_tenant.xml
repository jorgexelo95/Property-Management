<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/colorBlue"
    android:padding="20dp">


    <EditText
        android:id="@+id/edit_text_register_tenant_email"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textColor="@color/colorWhite"
        android:hint="Enter your email" />

    <EditText
        android:id="@+id/edit_text_register_landlord_email"
        android:layout_width="match_parent"
        android:textColor="@color/colorWhite"
        android:layout_height="wrap_content"
        android:hint="Enter your landlord's email" />

    <EditText
        android:id="@+id/edit_text_register_tenant_password"
        android:layout_width="match_parent"
        android:textColor="@color/colorWhite"
        android:layout_height="wrap_content"
        android:hint="Enter a password" />

    <Button
        android:id="@+id/button_register_tenant"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Register" />

</LinearLayout>